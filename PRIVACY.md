# プライバシー / Privacy

> **要約: 本ソフトウェアは、既定で入力内容を一切外部に送信しません。**
> 自動アップデート確認も既定 OFF で、設定画面から明示的に有効にした場合だけ実行します。
> 変換・学習・ログの全てがあなたの PC の中で完結します。
>
> *Summary: By default, this software never sends your input anywhere.
> All conversion, learning, and logging happen entirely on your PC.*

## 外部通信

- かな漢字変換（古典変換・Zenzai ニューラル変換）は**全てローカルで実行**されます。
  ニューラル変換はローカル LLM（llama.cpp / Vulkan）を使います。外部LLM補正は製品設定では凍結中です。
- **自動アップデート確認は既定 OFF（opt-in）**です。設定でONにしたユーザーの更新確認タスクだけが
  GitHub Releases APIへHTTPSリクエストを送ります。更新確認は入力内容、変換履歴、辞書、学習データ、
  APIキーを送信しません。OFFの間はこの更新確認タスクによるDNS解決・通信・通知を行いません。
- 更新の手動確認とダウンロードは設定画面から操作した場合に実行します。更新通知だけでは
  インストールを開始しません。インストーラの実行にはWindowsの管理者確認が必要です。
- **Zenzai モデル（GGUF）のダウンロード**は、インストーラの既定選択タスク（解除可能）または
  設定アプリからの取得操作で行います。インライン予測モデルも設定アプリから取得します。取得先は
  Hugging Faceおよび本プロジェクトの固定リリース資産です。自動更新確認がOFFでも、これらの
  モデル取得は実行できます。モデルを取得しなくても古典変換は動作します。
- インライン予測は既定で無効です。有効化した場合も、確定済みの左文脈はローカルの
  `LLM-jp-3-150m` と `llama.cpp` だけで処理され、外部APIには送信されません。
- テレメトリ・クラッシュレポート・使用統計の自動送信は**ありません**。

## 学習データ

- 変換履歴からの学習データは `%LOCALAPPDATA%\nospacekey\memory`（ユーザー辞書は
  `%LOCALAPPDATA%\nospacekey\`）に**ローカル保存**され、外部送信されません。
- **パスワード／PIN入力欄では変換・学習・インライン予測を行いません。** パスワード欄（TSF の
  `IS_PASSWORD`、numeric/alphanumeric PIN scope /
  Chromium 系のパスワード専用コンテキスト）を検出すると直接入力モードに切り替わり、
  キー入力は合成・記録を経由せずそのままアプリケーションへ渡されます。
- 学習は設定アプリからオフにでき、蓄積済みの学習データもワンクリックで消去できます。

## 診断ログ

- 診断ログは**既定で無効**です。環境変数 `NOSPACEKEY_LOG=1` を設定した場合のみ
  `%TEMP%` 配下にローカル出力されます。自動で外部送信されることはありません
  （不具合報告に添付するかどうかはあなたが決めます）。
- インライン予測のログには要求、表示、受理、却下、timeout、処理時間、利用不能状態だけを記録し、
  入力文脈と予測本文は記録しません。モデルランタイムの追加診断も文字数と状態だけです。

## 設定データ

- 設定、ユーザー辞書、学習データはユーザー領域へローカル保存されます。設定内の API キーだけが
  DPAPI（Windows のユーザー単位暗号化）で保護されます。
