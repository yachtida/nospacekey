# プライバシー / Privacy

> **要約: 本ソフトウェアは、既定で入力内容を一切外部に送信しません。**
> 自動アップデート確認も既定 OFF で、設定画面から明示的に有効にした場合だけ実行します。
> 変換・学習・ログの全てがあなたの PC の中で完結します。
>
> *Summary: By default, this software never sends your input anywhere.
> All conversion, learning, and logging happen entirely on your PC.*

## 外部通信

- かな漢字変換（古典変換・Zenzai ニューラル変換）は**全てローカルで実行**されます。
  ニューラル変換もローカル LLM（llama.cpp / CPU）であり、クラウド変換 API は存在しません。
- ネットワークアクセスは、**Zenzai モデル（GGUF）のダウンロード**、設定画面から明示的に
  行う手動アップデート確認・ダウンロード、そして opt-in の自動アップデート確認に限られます。
  インストーラの Zenzai タスクは既定で選択され、通信主体である setup.exe が Hugging Face の公開
  モデル URL から約70MBの GGUF を `{app}\models\` へ取得します。タスクを解除すれば取得を
  スキップでき、失敗・キャンセル時もインストールは古典変換で続行します。設定画面からの取得は
  `NospacekeyConfig.exe` が同じ Hugging Face の取得先へ明示操作で接続し、ユーザー領域へ保存します。
  どの取得も行わなくても古典変換で全機能が動作します。
- 自動アップデート確認は既定 OFF（opt-in）です。有効にした場合、ON への切替直後または Config
  起動時の reconcile で best-effort の実行要求が出され、Task Scheduler はログオン30分後とその後
  約6時間ごとに GitHub Releases API（`api.github.com` の nospacekey 公開リポジトリ）へ問い合わせます。
  `update-state.json` の直近成功から6時間以内は checker が throttle します。
  通常の HTTPS リクエストに含まれる OS/HTTP ネットワーク情報、User-Agent、IP アドレス等が
  GitHub 側へ到達しますが、入力内容、変換履歴、辞書、学習データ、API キーは送信しません。
  自動確認を OFF にすると DNS 解決を含む自動通信は行いません。手動の更新確認・手動のモデル
  ダウンロードは、それぞれ設定画面で明示的に操作した場合だけ実行します（インストーラの既定選択
  タスクによる取得は別のインストール時フローです）。
- テレメトリ・クラッシュレポート・使用統計の送信は**ありません**。

## 学習データ

- 変換履歴からの学習データは `%LOCALAPPDATA%\nospacekey\memory`（ユーザー辞書は
  `%LOCALAPPDATA%\nospacekey\`）に**ローカル保存**され、外部送信されません。
- **パスワード入力欄では変換・学習を行いません。** パスワード欄（TSF の `IS_PASSWORD` /
  Chromium 系のパスワード専用コンテキスト）を検出すると直接入力モードに切り替わり、
  キー入力は合成・記録を経由せずそのままアプリケーションへ渡されます。
- 学習は設定アプリからオフにでき、蓄積済みの学習データもワンクリックで消去できます。

## 診断ログ

- 診断ログは**既定で無効**です。環境変数 `NOSPACEKEY_LOG=1` を設定した場合のみ
  `%TEMP%` 配下にローカル出力されます。自動で外部送信されることはありません
  （不具合報告に添付するかどうかはあなたが決めます）。

## 設定データ

- 設定、ユーザー辞書、学習データはユーザー領域へローカル保存されます。設定内の API キーだけが
  DPAPI（Windows のユーザー単位暗号化）で保護されます。
